[project]
name = "langchain-playground"
version = "0.1.0"
description = "LangChain agent patterns: tool-calling agent, chat model, and streaming copilot"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain[openai]>=1.3.15",
    "python-dotenv>=1.2.3",
]
